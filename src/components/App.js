import {BrowserRouter, Switch, Route} from 'react-router-dom';
export default function App() {
    return (
        <BrowserRouter>
            <header>Tô aqui</header>
            <Switch>

            </Switch>
        </BrowserRouter>
    );
}